from .play_game import *

def handle_key_press(key):
        # try:
        #     key_char = chr(key).upper()
        # except ValueError:
        #     return
        # for object in objects:
        #      if key_char == object_letter:
        #           print(f"get sliced")
        return